# 项目协作约定

- 优先进行精确、最小范围的修改，只读取与当前问题直接相关的文件片段。
- 避免输出大段日志、完整 diff 或与结论无关的工具结果。
- 除非用户明确要求，或修改风险确实需要，否则不运行完整测试；优先执行针对性检查。
- 不重复验证已经确认的内容，遇到环境、权限或工具问题时避免无意义地反复尝试。
- 需求中的关键术语存在歧义且会影响实现时，先用一句简短问题确认，再开始修改。
- 项目中如有安装使用 `antdv-superform` 包（源项目除外），编写或修改相关代码前，先阅读 `node_modules/antdv-superform/AI_GUIDE.md`，并遵循其中的现行 API、默认配置和推荐用法。
- 提问中如有误写会影响代码参数、属性等关键单词，需要提醒用户确认后再继续。
- 提交消息使用中文。
- 不要自动使用 `impeccable` skill；仅当用户明确调用 `$impeccable`、提到 `impeccable` 或请求其工作流子命令时使用。
