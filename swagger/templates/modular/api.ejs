<%
const { utils, route, config, modelTypes } = it;
const { _, pascalCase, require } = utils;
const apiClassName = pascalCase(route.moduleName);
const routes = route.routes;

const dataContracts = _.map(modelTypes, "name");
%>

<% if (dataContracts.length) { %>
import type { <%~ dataContracts.join(", ") %> } from "./<%~ config.fileNames.dataContracts %>"
<% } %>

<%
const { httpModule, httpPath, prePath } = config.fileNames
const pathArr = _.split(routes[0].request.path, '/')
const fristName = pathArr[1]
let moduleName = fristName
if (it.routes.combined.find(item => item.moduleName !== route.moduleName && item.routes[0].request.path.startsWith(`/${fristName}/`))) {
  moduleName += pascalCase(pathArr[2])
}
// 仅比较一级路径会遗漏 `/duty/item` 与 `/dutyItem/home` 这类拼接后重名的情况。
// 一旦默认命名冲突，使用第二段路径区分 API，确保 barrel export 可被安全加载。
const currentNameCount = it.routes.combined.filter((item) => {
  const itemPath = _.split(item.routes[0].request.path, '/')
  let itemName = itemPath[1]
  if (it.routes.combined.find(other => other.moduleName !== item.moduleName && other.routes[0].request.path.startsWith(`/${itemPath[1]}/`))) {
    itemName += pascalCase(itemPath[2])
  }
  return itemName === moduleName
}).length
if (currentNameCount > 1) {
  moduleName = fristName + pascalCase(pathArr[2])
}
%>

import { <%~ httpModule %> } from "<%~ httpPath %>";
/**
* <%~ routes[0].raw.tags.join(", ") %>

**/
export const <%= moduleName %>Api = <%= httpModule %>('<%= prePath + fristName %>', {
    <% routes.forEach((route) => { %>
        <% route.request.shortPath = _.replace(route.request.path, `/${fristName}`, '') %>
        <%~ includeFile('./procedure-call.ejs', { ...it, route }) %>
    <% }) %>
})
